import { MakeBolderPipe } from './make-bolder.pipe';

describe('MakeBolderPipe', () => {
  it('create an instance', () => {
    const pipe = new MakeBolderPipe();
    expect(pipe).toBeTruthy();
  });
});
