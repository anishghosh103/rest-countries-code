import { RegionFilterPipe } from './region-filter.pipe';

describe('RegionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RegionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
